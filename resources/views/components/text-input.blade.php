<input type="text" name="{{ $name }}" id="{{ $name }}" value="{{ $value }}"
    placeholder="{{ $placeholder }}"
    class="w-full text-sm rounded-md border border-slate-200 focus:ring-1 focus:ring-slate-600 px-2.5 py-1.5 placeholder:text-slate-400" />
